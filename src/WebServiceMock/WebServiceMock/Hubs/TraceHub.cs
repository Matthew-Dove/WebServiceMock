using Microsoft.AspNet.SignalR;

namespace WebServiceMock.Hubs
{
    /// <summary>Required by the SignalR framework so the server and clients can talk to each other over this TraceHub.</summary>
    public class TraceHub : Hub { }
}
